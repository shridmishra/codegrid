import "./globals.css";

export const metadata = {
  title: "Draw SVG Stroke On Scroll | Codegrid",
  description: "Draw SVG Stroke On Scroll | Codegrid",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
