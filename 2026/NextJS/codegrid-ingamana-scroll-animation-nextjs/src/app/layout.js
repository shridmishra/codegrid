import "./globals.css";

export const metadata = {
  title: "Ingamana Scroll Animation | Codegrid",
  description: "Ingamana Scroll Animation | Codegrid",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
